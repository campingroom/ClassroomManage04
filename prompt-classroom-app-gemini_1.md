# Prompt สำหรับสั่ง Gemini: โปรแกรมบริหารจัดการห้องเรียน

> วิธีใช้: ก๊อปข้อความตั้งแต่เส้นแบ่งด้านล่างไปจนจบ แล้ววางใน Gemini (แนะนำใช้โหมดที่สร้างโค้ดได้ เช่น Canvas) ถ้า Gemini ตอบยาวเกินจนพิมพ์ไม่จบ ให้พิมพ์ว่า "พิมพ์ต่อ" ได้เลย

---

## บทบาทของคุณ

คุณคือนักพัฒนาเว็บแอปมืออาชีพที่เชี่ยวชาญ Google Apps Script และ Google Sheets ช่วยผมสร้าง **เว็บแอปบริหารจัดการห้องเรียนสำหรับครู 1 คน** ที่ใช้งานได้จริง โค้ดต้องสมบูรณ์ พร้อมก๊อปไปวางใช้ได้ทันที และมีคำแนะนำการติดตั้งแบบทีละขั้นสำหรับคนที่ **ไม่เคยเขียนโค้ดมาก่อนและใช้เครื่อง Mac**

## เทคโนโลยีที่ต้องใช้

- **Google Apps Script** เป็นเว็บแอป (ใช้ `doGet`, HtmlService, `google.script.run`)
- **Google Sheets** เป็นฐานข้อมูลทั้งหมด (1 สเปรดชีต หลายชีตย่อยทำหน้าที่เป็นตาราง)
- หน้าเว็บต้อง **Responsive** ใช้ได้สวยทั้งคอมพิวเตอร์ แท็บเล็ต/ไอแพด และโทรศัพท์
- ทำเป็น **PWA** ให้เพิ่มลงหน้าจอโฮมได้ และ **เช็กชื่อแบบออฟไลน์** ได้ แล้วค่อยซิงก์เข้า Sheets เมื่อมีเน็ต
- ใช้ภาษาไทยทั้งหมดในหน้าจอ ดีไซน์สะอาด อ่านง่าย ปุ่มใหญ่กดสะดวกบนมือถือ
- เมื่อรันครั้งแรก ให้สคริปต์ **สร้างชีตและหัวคอลัมน์ที่จำเป็นให้อัตโนมัติ** ถ้ายังไม่มี

## โครงสร้างข้อมูล (ชีตย่อยในสเปรดชีต)

ออกแบบให้ "นักเรียน" เป็นบัญชีกลางชุดเดียว แล้วผูกเข้ากับแต่ละชั้นเรียน เพื่อไม่ต้องกรอกซ้ำ

1. **Settings** — ปีการศึกษาและภาคเรียนที่กำลังใช้งาน, เกณฑ์ตัดเกรดเริ่มต้น
2. **Terms** — ปีการศึกษา/ภาคเรียนทั้งหมด (รองรับหลายเทอม เก็บข้อมูลข้ามเทอมได้)
3. **Subjects** — วิชา: รหัสวิชา, ชื่อวิชา, หน่วยกิต, สัดส่วนคะแนนเต็มของแต่ละก้อน, เกณฑ์ตัดเกรดเฉพาะวิชา
4. **Rooms** — ห้อง/ชั้นเรียน เช่น ม.1/1, ม.2/3
5. **Classes** — ชั้นเรียน = วิชา × ห้อง × เทอม (1 วิชาเปิดได้หลายห้อง) แต่ละแถวคือคาบที่ครูสอนจริง
6. **Students** — บัญชีนักเรียนกลาง: เลขที่, รหัสนักเรียน, ชื่อ-สกุล, ห้อง, รูป (ไม่บังคับ)
7. **Enrollments** — นักเรียนคนไหนอยู่ในชั้นเรียนไหน (เชื่อม Students กับ Classes)
8. **Attendance** — การเช็กชื่อ: ชั้นเรียน, วันที่, นักเรียน, สถานะ
9. **ScoreItems** — ชิ้นงาน/หัวข้อคะแนน: ชั้นเรียน, กลุ่มคะแนน (เก็บก่อนกลาง/กลางภาค/เก็บก่อนปลาย/ปลายภาค), ชื่อหัวข้องาน, คะแนนเต็ม
10. **Scores** — คะแนนของนักเรียนแต่ละคนต่อแต่ละชิ้นงาน
11. **Behavior** — บันทึกพฤติกรรม/โน้ตรายบุคคล
12. **Backups** — บันทึกประวัติการสำรองข้อมูล

## ฟีเจอร์ที่ต้องมีทั้งหมด

### จัดการวิชาและชั้นเรียน
- สร้าง/แก้ไข/ลบ ได้หลายวิชาและหลายห้อง
- วิชาเดียวเปิดได้หลายห้อง

### จัดการนักเรียน
- **อัปโหลดทีละมาก** จากไฟล์ CSV หรือวางจากตาราง (paste) พร้อมตัวอย่างหัวคอลัมน์ที่ถูกต้อง
- เพิ่ม/แก้ไข/ลบ นักเรียนรายบุคคลได้
- ย้ายนักเรียนเข้า-ออกชั้นเรียนได้

### เช็กชื่อ (Attendance)
- เลือกชั้นเรียนและวันที่ แล้วเช็กชื่อทีละคน
- สถานะแบบไทย 5 แบบ: **มา / มาสาย / ลากิจ / ลาป่วย / ขาด**
- เก็บสถิติการมาเรียนรายคนและรายห้อง (จำนวนครั้งและเปอร์เซ็นต์)
- **แก้ไขข้อมูลย้อนหลังได้** เลือกวันที่เก่ามาแก้ได้
- มีปุ่มพิมพ์/ส่งออกใบรายชื่อเช็กชื่อ

### คะแนนและตัดเกรด (Gradebook)
- 4 กลุ่มคะแนน: คะแนนเก็บก่อนกลางภาค, สอบกลางภาค, คะแนนเก็บก่อนปลายภาค, สอบปลายภาค
- คะแนนเก็บ **กำหนดหัวข้อของแต่ละชิ้นงานเองได้** และกำหนดคะแนนเต็มแต่ละชิ้นได้
- **สัดส่วนคะแนนเต็มปรับเองได้รายวิชา** ค่าเริ่มต้น เก็บก่อนกลาง 25 + กลางภาค 20 + เก็บก่อนปลาย 25 + ปลายภาค 30 = 100
- คำนวณ **คะแนนรวม** อัตโนมัติ
- **ตัดเกรด 8 ระดับ** เริ่มต้น: 4=80, 3.5=75, 3=70, 2.5=65, 2=60, 1.5=55, 1=50, ต่ำกว่า 50=0 (ปรับเกณฑ์รายวิชาได้)
- เพิ่ม **คะแนนจิตพิสัย/คุณลักษณะ** ที่เลือกได้ว่าจะนับรวมในเกรดหรือไม่

### ดูแลนักเรียน
- **แจ้งเตือนนักเรียนกลุ่มเสี่ยงอัตโนมัติ**: ไฮไลต์เด็กที่ขาดเรียนเกินเกณฑ์ที่ตั้งไว้ หรือคะแนนรวมต่ำกว่าเกณฑ์
- **บันทึกพฤติกรรมรายบุคคล**: โน้ตสั้น ๆ ต่อนักเรียนแต่ละคน พร้อมวันที่

### แดชบอร์ด
- **แดชบอร์ดรวม** หน้าแรก: ภาพรวมทุกวิชา จำนวนนักเรียน เปอร์เซ็นต์การมาเรียนเฉลี่ย การกระจายเกรด รายชื่อกลุ่มเสี่ยง
- **แดชบอร์ดย่อยในแต่ละหัวข้อ**: หน้าเช็กชื่อมีสรุปสถิติการมาเรียน, หน้าคะแนนมีกราฟกระจายเกรดและคะแนนเฉลี่ย ฯลฯ

### ข้อมูลและการสำรอง
- ข้อมูลทุกอย่าง **เพิ่ม แก้ไข อัปเดต และลบ (CRUD) ได้ครบ**
- **ระบบสำรองข้อมูลแบบมีเวอร์ชัน**: ปุ่มสำรองข้อมูล ณ ปัจจุบัน (สร้างสำเนาชีตพร้อมประทับวันเวลา) และปุ่มกู้คืนกลับไปเวอร์ชันเก่าได้

### ส่งออก
- ส่งออกเป็น **Google Sheet** (สร้างเป็นไฟล์ใหม่ในไดรฟ์)
- ส่งออกเป็น **Excel (.xlsx)**
- ส่งออกเป็น **PDF** (เช่น ใบรายชื่อ, สรุปคะแนน, รายงานการมาเรียน)
- เพิ่มการส่งออกแบบ **ปพ.5** (แบบบันทึกผลการเรียนประจำวิชา) ให้ใกล้เคียงรูปแบบราชการมากที่สุด

## ข้อกำหนดด้านการใช้งาน

- เป็นแอปสำหรับครูคนเดียว ผูกกับบัญชี Google ของผมเอง ไม่ต้องมีระบบล็อกอินผู้ใช้หลายคน
- เมนูนำทางชัดเจน สลับระหว่าง 7 หน้าได้ง่าย: แดชบอร์ดรวม / วิชาและชั้นเรียน / นักเรียน / เช็กชื่อ / คะแนน / รายงานและส่งออก / ตั้งค่าและสำรองข้อมูล
- ใช้งานบนมือถือได้จริง โดยเฉพาะหน้าเช็กชื่อต้องกดเร็ว

## สิ่งที่ขอให้ส่งกลับมา

1. โครงสร้างชีตทั้งหมดพร้อมหัวคอลัมน์ของแต่ละชีต
2. โค้ดครบทุกไฟล์ (เช่น `Code.gs`, `Index.html`, ไฟล์ JS/CSS, ไฟล์ PWA) แบบเต็มพร้อมก๊อปวางได้ทันที
3. คำอธิบายสั้น ๆ ว่าแต่ละไฟล์ทำอะไร
4. **ขั้นตอนการติดตั้งและนำไปใช้ทีละขั้นแบบละเอียดสำหรับมือใหม่บน Mac** ตั้งแต่สร้างสเปรดชีต เปิด Apps Script วางโค้ด ตั้งค่า Deploy เป็น Web App ไปจนถึงเปิดใช้บนมือถือ
5. ตัวอย่างไฟล์ CSV สำหรับอัปโหลดนักเรียน (หัวคอลัมน์ + ตัวอย่าง 2-3 แถว)

ถ้างานใหญ่เกินตอบในครั้งเดียว ให้ทยอยส่งเป็นส่วน ๆ โดยเริ่มจากโครงสร้างชีตและโครงหลักของแอปก่อน แล้วบอกผมให้พิมพ์ "ต่อ" เพื่อรับส่วนถัดไป
